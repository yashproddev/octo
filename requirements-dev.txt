# Local development only — deliberately NOT in requirements.txt so they stay
# out of the Vercel serverless bundle.
-r requirements.txt
uvicorn[standard]>=0.30
alembic>=1.13,<2.0
pytest>=8.0
httpx>=0.27
